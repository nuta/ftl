name: echo
kind: interface/v0
spec:
  messages:
  - name: ping
    context: control
    type: call
    params:
    - { name: value, type: int32, help: "The value to return" }
    returns:
    - { name: value, type: int32, help: "The value returned" }
