{
  "name": "tcpip",
  "kind": "app/v0",
  "spec": {
    "depends": [
      {
        "name": "ethernet_device",
        "type": "service",
        "interface": "ethernet_device"
      }
    ],
    "provides": [
      "tcpip"
    ]
  }
}
