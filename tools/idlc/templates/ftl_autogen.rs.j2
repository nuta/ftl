use ftl_api::message::MessageBody;
use ftl_api::message::StringField;

pub mod protocols {
    {% for m in messages %}
    #[repr(C)]
    pub struct {{ m.name }} {
        {% for f in m.fields %}
        pub {{ f.name }}: {{ f.ty }},
        {% endfor %}
    }
    {% endfor %}
}
