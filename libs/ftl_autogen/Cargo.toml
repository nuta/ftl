[package]
name = "ftl_autogen"
version = "0.0.0"
edition = "2021"

[lib]
path = "lib.rs"

[dependencies]
ftl_api = { path = "../../api" }
ping = { path = "../../fibers/ping" }
pong = { path = "../../fibers/pong" }
